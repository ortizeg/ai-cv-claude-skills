[skill]
name = "wandb"
version = "1.0.0"
category = "experiment-tracking"
tags = ["experiment-tracking", "visualization", "wandb", "metrics"]

[dependencies]
requires = ["loguru"]
recommends = ["pytorch-lightning"]

[compatibility]
python = ">=3.11"

[compatibility.libraries]
wandb = ">=0.16"
