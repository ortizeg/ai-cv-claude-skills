[skill]
name = "vscode"
version = "1.0.0"
category = "infra"
tags = ["editor", "ide", "vscode", "configuration"]

[dependencies]
requires = []
recommends = ["code-quality"]

[compatibility]
python = ">=3.11"
libraries = {}
