[skill]
name = "dvc"
version = "1.0.0"
category = "infra"
tags = ["data-versioning", "dvc", "model-versioning", "pipelines"]

[dependencies]
requires = ["loguru"]
recommends = ["gcp"]

[compatibility]
python = ">=3.11"

[compatibility.libraries]
dvc = ">=3.0"
